apiVersion: v1
kind: Service
metadata:
  name: {{ include "clock-server.fullname" . }}
  labels:
    {{- include "clock-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clock-server.selectorLabels" . | nindent 4 }}
